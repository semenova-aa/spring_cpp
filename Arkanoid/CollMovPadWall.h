
#pragma once
#include <SFML/Window.hpp>
#include <SFML/Graphics.hpp>
#include "MovingPaddle.h"
#include "Ball.h"

using namespace std;
using namespace sf;

void testCollision(MovingPaddle& mPaddle) {
	/*if (mPaddle.x() < 0)
		mPaddle.velocity.x = movingPaddleVelocity;
	else if (mPaddle.x() > windowWidth)
		mPaddle.velocity.x = -movingPaddleVelocity;*/

}